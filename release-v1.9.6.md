# Tianphoto v1.9.6

这次更新把最近几轮最关键的改动收拢成了一个更稳定也更顺手的版本。

## 本版重点

- 编辑器工具栏做减法，移除了不实用的字体设置入口
- 新增左对齐 / 居中 / 右对齐按钮，排版操作更直觉
- 编辑器视觉升级为浮动玻璃坞，导出预览弹窗也改成更完整的宽幅舞台
- 默认输出目录改为桌面 `tianphoto-iterations`，每次 HTML 自动带时间戳，方便连续迭代
- 新增全新风格家族 `studio-ribbon` 与预设 `opal-ribbon`

## 最近更新总览

### 1. 导出所见即所得继续收敛

- 导出主链已经统一到更稳定的页面内渲染路径
- 导出预览改成主舞台 + 缩略条，不再把长图硬塞进狭窄列表
- 默认打开完整图的原始宽度视图，检查细节更直接
- 根容器不再进入 `contenteditable`，避免浏览器编辑态尾白影响导出高度

### 2. 编辑器交互回到高频核心

- 保留撤销、重做、加粗、斜体、列表、引用、插图、保存、导出
- 补齐段落对齐能力
- 删除字体工具，避免复杂但低收益的编辑路径继续干扰使用

### 3. 审美层继续升级

- 工具栏从贴边条改为浮动玻璃坞
- 导出弹窗改成更有舞台感的宽幅结构
- 新增 `opal-ribbon`，适合品牌发布、案例展示、设计简报这类内容

## 升级提示

- 旧的 `*-page.html` 是自包含文件，里面内联的是旧版编辑器脚本
- 想使用这次的新工具栏、新预览和新风格，需要重新生成一次 HTML
